# v3.10.3 发布同步计划：权限修复与品牌资源403修复

> 文档版本：v1.0  更新日期：2026-08-10

---

## 一、当前状态

| 项目 | 当前值 |
|------|--------|
| VERSION | 3.10.2 |
| frontend/package.json | 3.10.2 |
| 最新 tag | v3.10.2 |
| 最新 commit | 7df7d70 (main), develop 与 origin/develop 同步 |
| 未提交修改 | 4 个文件 |

### 未提交的代码修改

| 文件 | 变更内容 |
|------|----------|
| [backend/Dockerfile](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/backend/Dockerfile) | 新增 `mkdir -p /var/log/tam /app/uploads/backups /app/uploads/branding` + `chown app:app`，修复 named volume 首次挂载为 root:root 导致 app 用户无法写入 |
| [docker-compose.yml](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docker-compose.yml) | 删除 4 处 `Production hardening` 注释块 + postgres/backend 冗余 tmpfs |
| [nginx/docker-entrypoint.sh](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/nginx/docker-entrypoint.sh) | 新增 tmpfs 目录 `chown nginx:nginx` 权限调整，修复自定义 entrypoint 覆盖官方权限初始化 |
| [nginx/etc/conf.d/tam.conf.template](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/nginx/etc/conf.d/tam.conf.template) | 移除 `valid_referers` 检查块，修复生产环境通过 IP 访问时品牌资源加载 403 |

---

## 二、版本号决策

**3.10.2 → 3.10.3**（patch 级修复）

理由：本次修改均为 bug fix（权限修复 + 403 修复），无新功能、无 breaking change，符合 semver patch 递增规则。

---

## 三、执行步骤

### 步骤 1：版本更新（3 个文件）

| 文件 | 修改 |
|------|------|
| [VERSION](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/VERSION) | `3.10.2` → `3.10.3` |
| [frontend/package.json](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/frontend/package.json) | `"version": "3.10.2"` → `"version": "3.10.3"` |
| [frontend/package-lock.json](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/frontend/package-lock.json) | `"version": "3.10.2"` → `"version": "3.10.3"`（2 处：root + packages."" 节点） |

### 步骤 2：文档更新（5 个文件）

#### 2.1 [docs/changelog.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/changelog.md)

- 头部文档版本：`v3.10.2` → `v3.10.3`，更新日期 → `2026-08-10`
- 在 `## [3.10.2]` 之前新增 `## [3.10.3] - 2026-08-10` 版本块：

```markdown
## [3.10.3] - 2026-08-10

### 修复

- **品牌资源加载 403**：Nginx `/uploads/` location 中的 `valid_referers` 检查在 `server_name _` 配置下无法匹配生产环境 IP 访问的 Referer，导致品牌背景图和 favicon 加载返回 403
  - 修复方案：移除 `valid_referers` + `if ($invalid_referer)` 检查块；品牌资源文件名使用 UUID 生成不可猜测，无需 Referer 访问控制
  - 关联文件：nginx/etc/conf.d/tam.conf.template

- **容器 volume 挂载权限**：取消容器安全加固后，Docker named volume 首次挂载以 root:root 创建挂载点目录，app 用户（非 root）无法写入 `/var/log/tam` 和 `/app/uploads`
  - 修复方案：backend/Dockerfile 在 `USER app` 前预创建 `/var/log/tam`、`/app/uploads/backups`、`/app/uploads/branding` 并设置 `app:app` 所有者；nginx/docker-entrypoint.sh 补充 tmpfs 目录 `chown nginx:nginx`
  - 关联文件：backend/Dockerfile、nginx/docker-entrypoint.sh

### 改进

- **清理安全加固残留配置**：删除 docker-compose.yml 中 4 处 `Production hardening` 注释块和 postgres/backend 冗余 tmpfs（取消 read_only 后不再需要）
  - 关联文件：docker-compose.yml
```

#### 2.2 [docs/release-notes.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/release-notes.md)

- 头部文档版本：`v3.10.2` → `v3.10.3`，更新日期 → `2026-08-10`
- 在 `## [v3.10.2]` 之前新增 `## [v3.10.3] - 2026-08-10` 版本块：

```markdown
## [v3.10.3] - 2026-08-10

### 权限修复与品牌资源 403 修复包

#### 背景

v3.10.2 取消容器安全加固后，遗留两类问题：
1. Docker named volume 首次挂载以 root:root 创建目录，app 用户无法写入日志和上传目录
2. Nginx `/uploads/` 的 `valid_referers` 检查在 `server_name _` 下无法匹配 IP 访问，品牌资源加载 403

#### 修改文件

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| backend/Dockerfile | 新增 | 预创建 /var/log/tam、/app/uploads/backups、/app/uploads/branding + chown app:app |
| docker-compose.yml | 删除 | 4 处安全加固注释块 + postgres/backend 冗余 tmpfs |
| nginx/docker-entrypoint.sh | 新增 | tmpfs 目录 chown nginx:nginx 权限调整 |
| nginx/etc/conf.d/tam.conf.template | 删除 | valid_referers 检查块 |
| docs/changelog.md | 更新 | 新增 [3.10.3] 版本块 |
| docs/release-notes.md | 更新 | 新增 [v3.10.3] 版本块 |
| docs/production-readiness-assessment.md | 更新 | 文档版本跟踪表 |
| docs/deployment.md | 更新 | 文档版本号 |
| docs/branding.md | 更新 | 文档版本号 |
| VERSION | 更新 | 3.10.2 → 3.10.3 |
| frontend/package.json | 更新 | 3.10.2 → 3.10.3 |
| frontend/package-lock.json | 更新 | 3.10.2 → 3.10.3 |

#### 验证

- backend 容器内 /var/log/tam/app.log 正常写入，所有者 app:app
- /app/uploads/backups/ 和 /app/uploads/branding/ 目录可写
- Nginx 启动无 Permission denied 错误
- 浏览器通过 IP 访问登录页，品牌背景图和 favicon 加载 200 OK
```

#### 2.3 [docs/production-readiness-assessment.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md)

- 头部文档版本：`v3.10.0` → `v3.10.3`，更新日期 → `2026-08-10`
- 文档版本跟踪表新增一行：

```markdown
| v3.10.3 | 2026-08-10 | v3.10.3 | 容器安全加固取消后权限修复、品牌资源 403 修复 | ✓ | ✓ | 待发布 |
```

#### 2.4 [docs/deployment.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md)

- 头部文档版本：`v3.9.0` → `v3.10.3`，更新日期 → `2026-08-10`

#### 2.5 [docs/branding.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/branding.md)

- 头部文档版本：`v3.6.18` → `v3.10.3`，更新日期 → `2026-08-10`

### 步骤 3：Git 提交（develop 分支）

```bash
git add backend/Dockerfile docker-compose.yml nginx/docker-entrypoint.sh \
  nginx/etc/conf.d/tam.conf.template VERSION frontend/package.json \
  frontend/package-lock.json docs/changelog.md docs/release-notes.md \
  docs/production-readiness-assessment.md docs/deployment.md docs/branding.md
```

提交信息：
```
fix(infra): volume permissions and branding 403 fix (v3.10.3)

- Pre-create /var/log/tam, /app/uploads/backups, /app/uploads/branding
  with app:app ownership in backend Dockerfile
- Add tmpfs chown nginx:nginx in nginx docker-entrypoint.sh
- Remove valid_referers check causing 403 on branding assets via IP access
- Clean up hardening comment blocks and redundant tmpfs in docker-compose.yml
- Bump version to 3.10.3, update 5 docs
```

### 步骤 4：推送 develop 到远程

```bash
git push origin develop
```

### 步骤 5：用户手动创建 PR（develop → main）

- **PR 标题**：`v3.10.3: 权限修复与品牌资源403修复`
- **PR 内容**：简要描述 4 个代码文件修改 + 5 个文档更新 + 3 个版本文件更新
- 用户在 GitHub Web 页面手动创建并合并

### 步骤 6：PR 合并后打 tag（用户确认合并后执行）

```bash
git checkout main
git pull origin main
git tag v3.10.3
git push origin v3.10.3
```

---

## 四、注意事项

1. **PR 必须先合并到 main，然后才能打 tag**（项目约束：Git tags 只能用于正式发布版本，且在 PR 合并到 main 后创建）
2. **VERSION 文件是版本唯一真相源**，package.json/package-lock.json 需同步更新
3. **manage.sh version check** 可用于验证版本一致性
4. **不执行 git pull**（manage.sh update 只做本地重建重启）
