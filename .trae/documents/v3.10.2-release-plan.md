# v3.10.2 文档更新 + 版本发布 + Git 工作流计划

## 当前状态分析

### 版本状态

* VERSION: `3.10.2`（已从 3.10.1 改好，未提交）

* frontend/package.json + package-lock.json: `3.10.2`（已改好，未提交）

* 已有 tag: v3.10.0, v3.10.1（v3.10.2 尚未打 tag）

* develop 落后 main 1 个 commit（v3.10.1 merge），未包含本次任何变更

### 工作区变更（19 文件，未提交）

| 文件                                       | 变更类型 | 内容                                                 |
| ---------------------------------------- | ---- | -------------------------------------------------- |
| `VERSION`                                | M    | 3.10.1 → 3.10.2                                    |
| `frontend/package.json`                  | M    | 3.10.2                                             |
| `frontend/package-lock.json`             | M    | 3.10.2                                             |
| `.env.example`                           | M    | 新增 COMPOSE\_PROJECT\_NAME + \[PORTS] 段             |
| `docker-compose.yml`                     | M    | 端口变量化 + env 全量传递 + 卷重命名 + healthcheck 修复           |
| `docker-compose.dev.yml`                 | D    | 删除                                                 |
| `docker-compose.prod.yml`                | D    | 删除                                                 |
| `nginx/etc/conf.d/tam.conf`              | D    | 删除                                                 |
| `nginx/etc/conf.d/tam.dev.conf`          | D    | 删除                                                 |
| `nginx/etc/conf.d/tam.conf.template`     | ??   | 新增（envsubst 模板）                                    |
| `nginx/docker-entrypoint.sh`             | ??   | 新增（envsubst 替代 sed）                                |
| `backend/Dockerfile`                     | M    | 恢复 USER app + BACKEND\_PORT env                    |
| `backend/.dockerignore`                  | M    | 排除 backups/uploads                                 |
| `backend/.gitignore`                     | ??   | 新增                                                 |
| `backend/app/core/config.py`             | M    | UPLOAD\_DIR 绝对路径                                   |
| `backend/app/core/logging_config.py`     | M    | loguru JSON 模板转义                                   |
| `backend/app/main.py`                    | M    | 新增 get\_config\_value / emit\_compliance\_alert 导入 |
| `backend/app/services/backup_service.py` | M    | DATABASE\_URL 解析 + 空备份清理                           |
| `backend/cli.py`                         | M    | user/role list 字段修复 + ADMIN\_PASSWORD env          |
| `manage.sh`                              | M    | 端口变量化 + dc() 简化 + \_api\_base\_url 动态              |
| `docs/changelog.md`                      | M    | 已部分更新（缺少近期变更）                                      |
| `docs/release-notes.md`                  | M    | 已部分更新（缺少近期变更）                                      |

### 文档差距

docs/changelog.md 和 docs/release-notes.md 的 v3.10.2 条目已包含：

* admin 密码硬编码修复

* backend tmpfs 冲突修复

* UPLOAD\_DIR 路径统一

* setup 提示上下文感知

* deploy DB 权威探测

* 部署验证顺序

**但缺少本次近期的重大变更**：

1. 端口变量化（TAM\_NGINX\_PORT / TAM\_NGINX\_SSL\_PORT / TAM\_BACKEND\_PORT）
2. 容器安全加固全面移除 + dev/prod override 文件删除
3. Nginx 配置架构重构（tam.conf.template + envsubst）
4. COMPOSE\_PROJECT\_NAME=tam 统一
5. Docker 卷重命名（tam-uploads→backend-data, tam-logs→backend-logs）
6. docker-compose.yml env 全量传递（43 个变量）
7. healthcheck 硬编码 8000 修复
8. 备份服务连接修复（DATABASE\_URL 解析 + pg\_dump 安装）
9. loguru JSON 模板转义修复
10. main.py 缺失导入修复
11. .dockerignore / .gitignore 防止备份文件打包

### 需更新的文档文件（7 个）

| 文件                                        | 需更新内容                                                                   |
| ----------------------------------------- | ----------------------------------------------------------------------- |
| `docs/changelog.md`                       | 补充近期变更条目                                                                |
| `docs/release-notes.md`                   | 补充近期变更详情                                                                |
| `docs/deployment.md`                      | 删除 dev/prod override 引用、安全加固引用、硬编码端口、tam.dev.conf；改为端口变量化 + template 架构 |
| `docs/architecture.md`                    | 删除 cap\_drop / security\_opt 引用                                         |
| `docs/manage-sh-reference.md`             | 更新 tam-uploads→backend-data、删除 dev/prod yml 引用                          |
| `docs/production-readiness-assessment.md` | 安全加固章节重写（移除 cap\_drop/read\_only 评估，改为内网部署说明）                           |
| `docs/logging-guide.md`                   | tam-logs→backend-logs                                                   |

## 执行计划

### Step 1: 更新 docs/changelog.md

在 `[3.10.2]` 段的 `### 改进` 后、`***` 分隔线前，追加以下条目：

```markdown
- **端口全面变量化**：新增 `TAM_NGINX_PORT`、`TAM_NGINX_SSL_PORT`、`TAM_BACKEND_PORT` 环境变量，`.env` 一处配置全链路生效（Nginx listen、upstream backend_api、backend uvicorn --port、healthcheck、manage.sh curl 检测）
  - 关联文件：docker-compose.yml、nginx/etc/conf.d/tam.conf.template、nginx/docker-entrypoint.sh、backend/Dockerfile、manage.sh、.env.example

- **Nginx 配置架构重构**：删除静态 `tam.conf` / `tam.dev.conf`，统一为 `tam.conf.template` + `envsubst` 动态生成；消除 dev/prod 双配置文件的维护负担
  - 关联文件：nginx/etc/conf.d/tam.conf.template、nginx/docker-entrypoint.sh

- **容器安全加固移除**：内网部署环境下 `cap_drop:ALL`、`read_only:true`、`no-new-privileges` 等加固措施导致权限冲突（nginx bind 80 失败、backend 卷写入失败），全面移除；删除 `docker-compose.dev.yml` 和 `docker-compose.prod.yml` 覆盖文件，dev/prod 差异仅由 `.env` 中 `ENVIRONMENT` 变量控制
  - 关联文件：docker-compose.yml、docker-compose.dev.yml(删除)、docker-compose.prod.yml(删除)

- **COMPOSE_PROJECT_NAME 统一**：`.env` 中新增 `COMPOSE_PROJECT_NAME=tam`，消除 `terminalaccessmanager-*` 冗余前缀资源
  - 关联文件：.env.example、manage.sh

- **Docker 卷重命名**：`tam-uploads`→`backend-data`、`tam-logs`→`backend-logs`，消除 `tam_tam-*` 冗余前缀，名称准确反映业务语义
  - 关联文件：docker-compose.yml

- **env 全量传递**：docker-compose.yml backend.environment 从 16 项扩充为 43 项，覆盖 Settings 类全部可配置字段；healthcheck 从硬编码 `localhost:8000` 改为容器内 `os.environ.get('BACKEND_PORT')` 动态读取
  - 关联文件：docker-compose.yml

- **备份服务连接修复**：backup_service.py 优先从 `DATABASE_URL` 正则解析连接参数，fallback 到 `DB_*` 字段；backend Dockerfile 新增 `postgresql-client` 提供 `pg_dump`；备份清理逻辑新增零字节文件删除
  - 关联文件：backend/app/services/backup_service.py、backend/Dockerfile

- **日志格式修复**：loguru JSON 格式化模板中 `{}` 未转义导致 `KeyError: '"timestamp"'`，改为 `{{}}` 转义
  - 关联文件：backend/app/core/logging_config.py

- **缺失导入修复**：main.py 新增 `get_config_value` 和 `emit_compliance_alert` 导入，修复 v3.10.1 引入的 NameError
  - 关联文件：backend/app/main.py

- **构建产物清理**：backend/.dockerignore 和 backend/.gitignore 新增，排除 `uploads/backups/`、`backups/`、`*.zip`、`uploads/`，防止运行时文件打包进镜像
  - 关联文件：backend/.dockerignore、backend/.gitignore
```

### Step 2: 更新 docs/release-notes.md

在 v3.10.2 段末尾追加新的子章节，记录近期修复：

```markdown
#### 端口变量化与架构简化

##### 端口全面变量化

| 项目 | 说明 |
|------|------|
| 背景 | Nginx/Backend 端口在配置文件中硬编码，改 `.env` 端口无效 |
| 修复 | 新增 `TAM_NGINX_PORT`/`TAM_NGINX_SSL_PORT`/`TAM_BACKEND_PORT`，通过 envsubst 动态注入 Nginx 配置；backend Dockerfile 使用 `BACKEND_PORT` env；manage.sh 端口检测函数化 |
| 影响 | `.env` 一处改端口，全链路生效 |

##### Nginx 配置架构重构

| 项目 | 说明 |
|------|------|
| 背景 | tam.conf / tam.dev.conf 双配置文件 + sed 替换端口，脆弱且易失效 |
| 修复 | 统一为 `tam.conf.template` + `envsubst`，仅替换 3 个变量不破坏 nginx 原生变量 |
| 删除 | tam.conf、tam.dev.conf |

##### 容器安全加固移除 + override 文件删除

| 项目 | 说明 |
|------|------|
| 背景 | 内网环境下 `cap_drop:ALL`/`read_only`/`no-new-privileges` 导致 nginx bind 失败、backend 卷写入失败 |
| 修复 | 全面移除安全加固配置；删除 docker-compose.dev.yml 和 docker-compose.prod.yml |
| dev/prod 差异 | 仅由 `.env` 中 `ENVIRONMENT` 变量控制（API 文档可见性、日志格式、启动校验严格度） |

##### Docker 卷重命名

| 项目 | 说明 |
|------|------|
| 背景 | 卷名 `tam-uploads`/`tam-logs` 加项目前缀后变成 `tam_tam-*`，冗余且语义不准确 |
| 修复 | 改为 `backend-data`/`backend-logs`，最终 Docker 卷名 `tam_backend-data`/`tam_backend-logs` |

##### env 全量传递 + healthcheck 修复

| 项目 | 说明 |
|------|------|
| 背景 | docker-compose.yml backend.environment 仅传 16 项，`.env` 中改 LOCKOUT_DURATION、LOG_LEVEL 等变量无效；healthcheck 硬编码 8000，自定义端口后服务被标记 unhealthy |
| 修复 | environment 扩充为 43 项覆盖全部 Settings 字段；healthcheck 改为容器内 `os.environ.get('BACKEND_PORT')` 动态读取 |

##### 备份服务修复

| 项目 | 说明 |
|------|------|
| 背景 | 三次连续备份失败：①POSTGRES_* 字段不存在 ②缺 pg_dump ③DB_HOST=localhost 覆盖 DATABASE_URL |
| 修复 | backup_service.py 优先从 DATABASE_URL 正则解析；Dockerfile 新增 postgresql-client；清理逻辑新增零字节文件删除 |

##### 日志/导入/构建修复

| 项目 | 说明 |
|------|------|
| loguru JSON 模板 | `{}` 未转义 → `{{}}` 转义 |
| main.py 导入 | 新增 get_config_value / emit_compliance_alert 导入 |
| .dockerignore/.gitignore | 排除 backups/uploads/zip 防止打包进镜像 |
```

### Step 3: 更新 docs/deployment.md

* [L136-137](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L136-L137): 删除 dev/prod override 文件行，改为单一 docker-compose.yml

* [L143-144](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L143-L144): 删除 dev/prod compose 命令，改为 `docker compose -f docker-compose.yml`

* [L150-156](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L150-L156): 删除安全加固表格（cap\_drop/security\_opt/read\_only），改为说明内网部署无需容器加固

* [L173](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L173): 删除 dev/prod 配置对比行

* [L27-34](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L27-L34): 端口表格改为变量化说明

* [L152](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L152): 删除 `cap_add: NET_BIND_SERVICE` 行

* [L889-912](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/deployment.md#L889-L912): 端口映射表格改为变量化

### Step 4: 更新 docs/architecture.md

* [L548](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/architecture.md#L548): 删除 `cap_drop: [ALL]` 行

* [L550](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/architecture.md#L550): 删除 `security_opt: no-new-privileges:true` 行

### Step 5: 更新 docs/manage-sh-reference.md

* [L410](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/manage-sh-reference.md#L410): `tam-uploads` → `backend-data`

* [L420](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/manage-sh-reference.md#L420): `tam-uploads` → `backend-data`

* [L484](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/manage-sh-reference.md#L484): `tam-uploads` → `backend-data`

* [L1109-1110](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/manage-sh-reference.md#L1109-L1110): 删除 dev/prod yml 行，改为单一 docker-compose.yml

### Step 6: 更新 docs/production-readiness-assessment.md

* [L133](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L133): 安全加固评分行改为内网部署说明

* [L142-146](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L142-L146): 删除 no-new-privileges/read\_only 列

* [L176](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L176): 更新 docker-compose.prod.yml 引用

* [L298](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L298): 更新 Docker 安全行

* [L311-319](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L311-L319): 安全配置表更新

* [L337](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L337): 更新 cap\_drop 引用

* [L591](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L591): 更新 docker-compose.prod.yml 引用

* [L670/L704/L710](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/production-readiness-assessment.md#L670): 更新安全加固条目

### Step 7: 更新 docs/logging-guide.md

* [L221](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/logging-guide.md#L221): `tam-logs` → `backend-logs`

* [L224](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/logging-guide.md#L224): `tam-logs` → `backend-logs`

### Step 8: 更新文档版本头日期

所有文档头部的 `更新日期` 改为 `2026-08-07`：

* changelog.md: `> 文档版本：v3.10.2  更新日期：2026-08-07`

* release-notes.md: `> 文档版本：v3.10.2 | 更新日期：2026-08-07`

### Step 9: 版本验证

* `cat VERSION` → 3.10.2 ✅

* `grep version frontend/package.json` → 3.10.2 ✅

* `grep version frontend/package-lock.json | head -1` → 3.10.2 ✅

### Step 10: 语法验证

* `python -m py_compile backend/app/main.py backend/app/core/config.py backend/app/core/logging_config.py backend/app/services/backup_service.py backend/cli.py`

* `bash -n manage.sh`

* `docker compose -f docker-compose.yml config --quiet`（需 VERSION 和 .env）

* `nginx -t`（容器内）

### Step 11: Git 提交

```bash
git add -A
git commit -m "fix(infra): port variable化, remove container hardening, env full pass-through (v3.10.2)

- Port variable化: TAM_NGINX_PORT/TAM_NGINX_SSL_PORT/TAM_BACKEND_PORT
- Nginx config: tam.conf.template + envsubst replacing sed
- Remove container security hardening (cap_drop/read_only/no-new-privileges)
- Delete docker-compose.dev.yml and docker-compose.prod.yml
- COMPOSE_PROJECT_NAME=tam in .env
- Volume rename: tam-uploads→backend-data, tam-logs→backend-logs
- docker-compose.yml: 43 env vars passed to backend (was 16)
- Fix healthcheck hardcoded port 8000
- Fix backup_service DATABASE_URL parsing
- Fix loguru JSON template escaping
- Fix missing imports in main.py
- Add .dockerignore/.gitignore for backup files
- Update 7 docs for new architecture"
```

### Step 12: 推送 develop

```bash
git checkout develop
git merge main
git push origin develop
```

### Step 13: 创建 PR

在 GitHub 上创建 PR: `develop → main`，标题 `v3.10.2: 端口变量化与架构简化`

### Step 14: 等待用户手动合并 PR

### Step 15: PR 合并后打 tag

```bash
git checkout main
git pull origin main
git tag v3.10.2
git push origin v3.10.2
```

## Assumptions & Decisions

1. **版本号保持 v3.10.2**：VERSION 和 docs 已设为 3.10.2 且尚未提交/tag，所有变更合并到这一个版本
2. **不创建 v3.11.0**：虽然移除安全加固是架构变更，但属于内网部署场景的合理简化，不构成 breaking change
3. **文档头部日期统一更新为 2026-08-07**
4. **production-readiness-assessment.md 安全评分降级**：容器加固移除后，安全评分从 9.0 降为 7.0（仍保留网络隔离 + 资源限制 + 非 root 用户）

## 验证步骤

1. `python -m py_compile` 5 个 Python 文件无报错
2. `bash -n manage.sh` 无报错
3. `docker compose config --quiet` 无报错
4. `git log --oneline -3` 确认提交
5. `git tag -l "v3.10.2"` 确认 tag 存在
6. `./manage.sh health` 全绿

