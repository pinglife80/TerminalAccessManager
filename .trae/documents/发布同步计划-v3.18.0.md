# 发布同步计划 v3.18.0（Minor）

> 文档版本：v3.18.0  更新日期：2026-09-02

## 一、概述

本计划将自 v3.17.3 发布（tag `v3.17.3` 已存在于 `main`，merge commit `39cc3bf` / PR #44）以来积累的未提交改动，按 [git-workflow-guide.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/git-workflow-guide.md) 单人开发最佳实践完成：**文档更新 → 版本号递增 → 代码提交 → 推送 develop → 手动 PR → 打 tag → develop 同步**。

主线为：**黑名单唯一口径收敛为 IP+防火墙（迁移 038）+ 终端去重/查重筛选（新功能） + 统计口径收敛与卡片简化 + 会话滑动续期修复**。

* **版本号**：`v3.18.0`（Minor，已与用户确认——因含「终端去重/查重筛选」新能力）
* **发布日期**：`2026-09-02`
* **发布分支**：`develop` → `main`
* **tag**：`v3.18.0`（annotated tag，仅打于 main，PR 合并后）

***

## 二、当前状态分析（发布依据）

### 2.1 仓库状态

| 项目 | 状态 |
| --- | --- |
| 当前分支 | `develop`，HEAD = `5dc4e93`（v3.17.3 发布提交），与 `origin/develop` 同点 |
| `main` | `39cc3bf`（Merge PR #44），与 `origin/main` 同点 |
| develop ↔ main 差集 | `git log develop..main` 仅含 merge commit `39cc3bf`（v3.17.3 发布尾步未执行 `develop merge main` 同步） |
| 最新 tag | `v3.17.3`（annotated，指向 main） |
| `VERSION` 文件 | `3.17.3`（待更新为 `3.18.0`） |
| 远端 | `git@github.com:pinglife80/TerminalAccessManager.git` |
| 未提交改动 | **15 个文件已修改 + 8 个未跟踪规划文档 + 1 个未跟踪迁移（038），全部位于 `develop` 工作区** |

> ✅ 与 v3.17.2（改动位于 `main` 工作区需迁移）不同，本次改动**已在 `develop` 工作区**，无需分支迁移，直接按「五、提交计划」在 develop 上提交即可。

### 2.2 待发布改动清单

#### 2.2.1 黑名单唯一口径收敛为 IP+防火墙（含迁移 038）

| 文件 | 变更要点 |
| --- | --- |
| `backend/alembic/versions/038_blacklist_unique_ip_firewall.py`（新增） | 去重历史活跃重复行（按 ip+firewall 保留最新，其余标记 unblocked）；删除旧唯一索引；重建 `(ip_address, firewall_tag)` 唯一索引 |
| `backend/app/models/blacklist.py` | 唯一索引 `idx_blacklist_unique_active` 由 `(ip, mac, firewall)` 收敛为 `(ip, firewall)` |
| `backend/app/services/compliance_service.py` | 新增 `_attach_active_blacklist`（按 `(ip, firewall)` 幂等插入/更新，DHCP 换 IP 原地更新 MAC，MAC 仅非空时覆盖）；两处封锁落点改用该方法；新增 compliant/bypass + 有残留 `block_state` 时的清理分支 |
| `backend/app/main.py` | retry-block 移除基于 MAC 的重复封锁检查，改用 `_attach_active_blacklist`（按 ip+firewall 幂等） |
| `backend/app/services/firewall_reconciliation_service.py` | active 口径移除 `expires_at` 过期剔除（过期窗口内不再误判孤儿）；孤儿重置不再写 `block_failed`（`block_state` 归属合规/封堵管线）；补建查询按 `(ip, firewall)` 唯一口径；补封闭成功后回写 `Terminal.status/firewall_tag` 并清 `block_state`（此前补封闭不回写终端表，导致终端滞留 unblocked + 陈旧 block_state） |

#### 2.2.2 统计口径收敛与卡片简化

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/services/terminal_service.py` | `get_blacklist_stats` 移除 `pending_retry_block`/`unblockable_non_compliant`；source 口径统一为启用 ARP 源；`pending_retry_unblock` 用 MAC 归一化 + `coalesce(mac_norm, ip)` 去重 |
| `backend/app/api/v1/endpoints/blacklist.py` | `export_blacklist` 的 `active_filter` 增加 `expires_at >= now OR is NULL` 检查，与列表/统计口径对齐 |
| `frontend/src/hooks/useTerminalData.ts` | `BlacklistStats` 移除 `pending_retry_block`/`unblockable_non_compliant`/`firewall_ip_count`；`DashboardStats` 移除 `non_compliant_unblocked`；`TerminalSearchParams` 的 `block_state`/`arp_enabled_only` → `dedup_dim`/`dedup_mode` |
| `frontend/src/pages/Blacklist.tsx` | 移除「待封锁/不可封锁非合规」统计卡片与跳转；网格 `xl:grid-cols-5`→`xl:grid-cols-4` |
| `frontend/src/i18n/locales/{en,zh,ja}.ts` | 删除 `statsPendingBlock*`/`statsPendingRetryBlock`/`statsUnblockable*`/`statsFirewallBlocked*`/`pendingRetryBlockHint` 等僵尸词条 |

#### 2.2.3 终端过滤精简 + 去重/查重筛选（新功能，Minor 依据）

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/schemas/terminal.py` | `block_state`/`arp_enabled_only` 字段移除，新增 `dedup_dim`/`dedup_mode` |
| `backend/app/api/v1/endpoints/terminals.py` | 查询参数同步：`block_state`/`arp_enabled_only` → `dedup_dim`/`dedup_mode` |
| `backend/app/services/terminal_service.py` | 新增 `_dedup_key_column`/`_duplicate_conditions`；`search` 支持按 ip/mac/both 维度 + dedup/duplicates 模式（aliased 子查询，SQLite 兼容）；移除 `block_state`/`arp_enabled_only` 过滤 |
| `frontend/src/pages/Terminals.tsx` | 新增「去重/查重维度」+「去重/查重模式」两个下拉框；移除 `block_state` 与「仅启用 ARP 源」过滤 |

#### 2.2.4 会话滑动续期修复

| 文件 | 变更要点 |
| --- | --- |
| `frontend/src/hooks/useTokenExpiration.ts` | token 自动续期（sliding session）仅关闭过期警告，**不再**重置 idle 计时器、不关闭 idle 超时警告，保证空闲用户仍会在超时后登出 |

#### 2.2.5 新增规划文档（未跟踪，纳入版本控制）

| 路径 | 说明 |
| --- | --- |
| `.trae/documents/终端数据一致性取证验证计划.md` | 取证验证计划 |
| `.trae/documents/终端管理过滤精简与去重查重筛选.md` | 终端过滤精简/去重查重方案 |
| `.trae/documents/统一黑名单唯一口径为IP+防火墙.md` | 黑名单唯一口径方案 |
| `.trae/documents/统计分类与数据口径说明.md` | 统计分类/口径说明 |
| `.trae/documents/统计卡片简化与黑名单口径对齐方案.md` | 卡片简化方案 |
| `.trae/documents/统计口径收敛改造计划.md` | 口径收敛改造计划 |
| `.trae/documents/统计口径收敛计划（终版）.md` | 口径收敛计划（终版） |
| `.trae/documents/统计口径简化改造计划.md` | 口径简化改造计划 |
| `.trae/documents/发布同步计划-v3.18.0.md` | 本计划文档 |

### 2.3 改动性质（决定 Minor 版本）

含 1 项面向用户的新能力（终端「去重/查重」筛选），故按语义化版本 Minor 递增至 `v3.18.0`；其余均为向后兼容的 bug 修复、统计口径收敛与 UI 精简。

***

## 三、版本更新

| 文件 | 当前 | 目标 |
| --- | --- | --- |
| `VERSION` | `3.17.3` | `3.18.0` |

> `VERSION` 是唯一版本源；`package.json` / `package-lock.json` 由前端镜像构建期动态同步，**无需手动改**（沿用既有约定）。

***

## 四、项目文档内容更新

遵循既有约定：文档头格式 `> 文档版本：{version}  更新日期：{date}`；`production-readiness-assessment.md` 仅追加跟踪表行、header 不 bump。

| 文档 | 更新方式 |
| --- | --- |
| `docs/changelog.md` | header `v3.17.3`→`v3.18.0`、日期 `2026-08-31`→`2026-09-02`；顶部新增 `## [3.18.0] - 2026-09-02` 条目，按「新增/修复/改进」分类，逐条附关联文件 |
| `docs/release-notes.md` | header `v3.17.3`→`v3.18.0`、日期同上；顶部新增 `## [v3.18.0] - 2026-09-02` 章节，含「背景/主要变更表/升级步骤/升级注意事项」 |
| `docs/production-readiness-assessment.md` | **仅**在文档版本跟踪表顶部追加 1 行 `v3.18.0`；header 保持 `v3.10.4` 不 bump（沿用既有约定） |
| `docs/business-workflow.md` | header `v3.17.3`→`v3.18.0`、日期同上（本次无业务流程变更，正文不改） |

### 4.1 待写入 changelog / release-notes 的变更条目

**新增（Features）**：

1. **终端去重/查重筛选**：终端管理页新增「去重/查重维度」(IP / MAC / IP+MAC) 与「去重/查重模式」(去重 / 查重) 两个下拉框，由服务端 SQL 按维度去重或筛出重复项，便于发现 DHCP 换 IP、同 IP 多终端等数据质量问题。关联：`terminal_service.py`、`endpoints/terminals.py`、`schemas/terminal.py`、`Terminals.tsx`

**修复（Fixes）**：

1. **黑名单唯一口径收敛为 IP+防火墙**（迁移 038）：唯一索引由 `(IP, MAC, 防火墙)` 收敛为 `(IP, 防火墙)`，与深信服按 IP 封堵、按 IP 幂等的真实行为对齐；去重历史重复活跃行；新增 `_attach_active_blacklist` 幂等写入（DHCP 换 IP 原地更新 MAC，仅非空覆盖）。关联：`038_blacklist_unique_ip_firewall.py`、`blacklist.py`(model)、`compliance_service.py`、`main.py`
2. **对账口径与补封闭收敛**：移除 `_repair_stale_terminal_status` 中写 `block_failed` 的逻辑（`block_state` 归属合规/封堵管线）；active 口径移除 `expires_at` 过期剔除，避免过期窗口误判孤儿导致封/解振荡；补封闭成功后回写 `Terminal.status/firewall_tag` 并清 `block_state`，修复补封闭后终端滞留 unblocked 的不一致。关联：`firewall_reconciliation_service.py`
3. **统计口径收敛与卡片简化**：`get_blacklist_stats` 移除 `pending_retry_block`/`unblockable_non_compliant`；`pending_retry_unblock` 用 MAC 归一化去重；黑名单导出 active_filter 补 `expires_at` 检查；前端移除「待封锁/不可封锁非合规」卡片与跳转。关联：`terminal_service.py`、`endpoints/blacklist.py`、`useTerminalData.ts`、`Blacklist.tsx`、i18n 三语
4. **终端过滤精简**：移除 `block_state` 与「仅启用 ARP 源」冗余过滤，统一为去重/查重维度/模式参数。关联：`schemas/terminal.py`、`endpoints/terminals.py`、`Terminals.tsx`
5. **滑动续期不再重置 idle**：token 自动续期仅关闭过期警告，不再重置 idle 计时器、不关闭 idle 超时警告。关联：`useTokenExpiration.ts`

***

## 五、代码提交计划（7 个提交，在 develop 上完成）

按主题分组，遵循 Conventional Commits（`type(scope): subject`）。提交顺序 1→7 依次执行，每个文件仅出现在一个提交中，避免分步 hunk 暂存（沿用仓库粗粒度提交风格）。

| # | 提交内容（文件） | Commit Message |
| --- | --- | --- |
| 1 | `backend/alembic/versions/038_blacklist_unique_ip_firewall.py`、`backend/app/models/blacklist.py` | `chore(database): migration 038 unify blacklist unique key to (ip, firewall)` |
| 2 | `backend/app/services/compliance_service.py`、`backend/app/main.py`、`backend/app/services/firewall_reconciliation_service.py` | `fix(blacklist): key active entries by IP+firewall and stop reconciliation writing block_state` |
| 3 | `backend/app/services/terminal_service.py`、`backend/app/api/v1/endpoints/terminals.py`、`backend/app/schemas/terminal.py`、`backend/app/api/v1/endpoints/blacklist.py` | `feat(terminals): add dedup/duplicate filters and converge stats calibers` |
| 4 | `frontend/src/hooks/useTokenExpiration.ts` | `fix(session): keep sliding token refresh from resetting idle timeout` |
| 5 | `frontend/src/pages/Terminals.tsx`、`frontend/src/pages/Blacklist.tsx`、`frontend/src/hooks/useTerminalData.ts`、`frontend/src/i18n/locales/en.ts`、`frontend/src/i18n/locales/zh.ts`、`frontend/src/i18n/locales/ja.ts` | `feat(frontend): add dedup/duplicate dropdowns and simplify blacklist stat cards` |
| 6 | `.trae/documents/` 8 个规划文档 + `发布同步计划-v3.18.0.md` | `docs: add v3.18.0 convergence and dedup-filter plan documents` |
| 7 | `VERSION`、`docs/changelog.md`、`docs/release-notes.md`、`docs/production-readiness-assessment.md`、`docs/business-workflow.md` | `chore(release): bump version to v3.18.0 and refresh docs` |

***

## 六、推送与 PR

1. **提交前验证**（在 develop 上）：
   ```bash
   ./manage.sh update          # 本地重建 + 重启，确认构建/TS 编译无错
   docker exec tam_backend python -m pytest -q   # 后端回归（预期全绿）
   ```

2. **按「五、提交计划」依次 commit**，每个提交后用 `git status` 确认无漏提/误提。

3. **推送 develop**：`git push origin develop`（含提交 1–7，共 7 个）。

4. **手动创建 PR**（GitHub：`develop` → `main`）：
   * 标题：`release: v3.18.0 黑名单唯一口径收敛、终端去重查重与统计口径简化`
   * 描述：粘贴 `docs/changelog.md` 的 `[3.18.0]` 条目
   * 复核 `backend-lint / backend-test / frontend-lint / frontend-test` 四项 CI 均通过
   * 自审 diff 后选择 **Merge**（Merge commit，沿用既有习惯；不 squash、不删除 develop）

***

## 七、打 tag 与 develop 同步

> 依据项目约束「Git tag 仅打于 main、且须 PR 合并后」。

1. PR 合并后，本地同步并切到 main：
   ```bash
   git checkout main && git pull origin main
   ```
2. 打 annotated tag 并推送：
   ```bash
   git tag -a v3.18.0 -m "release v3.18.0: blacklist IP+firewall convergence, terminal dedup filter and stats simplification"
   git push origin v3.18.0
   ```
3. develop 同步 main（含 tag；顺带补上 v3.17.3 遗留未同步的 merge commit `39cc3bf`）：
   ```bash
   git checkout develop && git merge main && git push origin develop
   ```

***

## 八、假设与决策

1. **版本号**：`v3.18.0`（Minor）——已与用户确认；因含「终端去重/查重筛选」新能力，其余为向后兼容修复/收敛。
2. **文档命名**：`发布同步计划-v3.18.0.md`——与 v3.18.0 版本号一致，延续既有《发布同步计划-vX.Y.Z.md》命名。
3. **改动位置**：未提交改动已在 `develop` 工作区，无需分支迁移。
4. **提交粒度**：7 个提交（数据库迁移 / 黑名单幂等与对账 / 终端去重查重+统计 / 会话续期 / 前端 / 规划文档 / 版本发布），与仓库既有粗粒度风格一致。
5. **`production-readiness-assessment.md` header 保持 v3.10.4 不 bump**，仅追加跟踪表行（沿用既有约定）。
6. **`package.json` 不改**：由前端镜像构建期从 `VERSION` 动态同步。
7. **PR 采用手动创建**（用户明确「手动 PR」），合并动作在 GitHub 上完成。
8. **规划文档纳入版本控制**：8 个既有计划文档 + 本计划文档一并提交。
9. **已知缺口（不阻塞本计划）**：`backend/tests/` 当前**不含「终端去重/查重」专项单元测试**；若需补齐，可并入提交 3 或单列 `test(terminals)` 提交，由用户决定是否纳入本次发布。

***

## 九、验证步骤（发布后）

1. `git tag -l | tail` 确认 `v3.18.0` 存在。
2. `git log v3.17.3..v3.18.0 --oneline` 核对版本间提交与文件清单。
3. `git describe --tags --abbrev=0` 返回 `v3.18.0`。
4. `./manage.sh health` 确认服务健康、版本号已刷新为 `3.18.0`。
5. 迁移验证：`./manage.sh upgrade` 执行 `alembic upgrade head` 后，`docker exec tam_backend alembic current` 应显示 `038_blacklist_unique_ip_firewall`。
6. 业务链抽查：
   * 终端管理页切换「去重/查重维度/模式」，确认列表按维度去重/筛重且无白屏刷新。
   * 黑名单页确认仅剩 4 张统计卡（成功封锁 / 成功解封 / 解封失败待重试 / 防火墙异常），无「待封锁」跳转。
   * 同一 IP 换 MAC 场景（DHCP 换 IP）不再产生重复活跃黑名单行（唯一索引生效）。