# v3.13.0 发布计划

> 计划制定日期：2026-08-21
> 当前分支：develop
> 当前版本：v3.12.1

---

## 0. 当前状态

- 当前在 develop 分支
- 本地代码就是最新的，无需拉取
- 有 13 个文件已修改未提交

---

## 1. 变更概述

### 1.1 修改文件
```
backend/app/api/v1/endpoints/system.py
backend/app/main.py
backend/app/services/compliance_service.py
backend/app/services/firewall_reconciliation_service.py
backend/app/services/sangfor_service.py
backend/app/services/terminal_service.py
frontend/src/hooks/useTerminalData.ts
frontend/src/i18n/locales/en.ts
frontend/src/i18n/locales/ja.ts
frontend/src/i18n/locales/zh.ts
frontend/src/lib/constants.ts
frontend/src/pages/Blacklist.tsx
frontend/src/pages/Terminals.tsx
```

---

## 2. 发布流程（严格按用户要求）

### 步骤 1：更新文档和版本文件

#### 1.1 更新 changelog.md

编辑 `docs/changelog.md`，在顶部添加：

```markdown
## [3.13.0] - 2026-08-21

### 新增

- **防火墙实际统计展示**：
  - 新增 `_cache_reconcile_result` 函数，将对账结果缓存到 Redis（TTL 1 小时）
  - 黑名单页面新增"防火墙实际封锁数"统计卡片
  - 手动触发对账端点也会缓存结果
  - 关联文件：`main.py`、`system.py`、`Blacklist.tsx`

### 修复

- **黑名单统计不一致问题**：
  - 统一三个页面统计口径：Dashboard、终端管理、黑名单都使用 `blocked` 计数
  - 终端管理页面从 `stats.non_compliant` 改为 `stats.blocked`
  - 移除 `firewall_reconciliation_service.py` 中重复的缓存逻辑，统一使用 `main.py` 实现
  - 修复前端 fallback 统计逻辑：从 `compliance_status` 改为 `status === 'blocked'`
  - 关联文件：`firewall_reconciliation_service.py`、`Terminals.tsx`、`terminal_service.py`

- **状态不变量加强**：
  - 确保 `compliance_status='non_compliant'` 意味着 `status='blocked'`
  - 冷却期阻止合规状态降级而非阻止封锁动作
  - non_compliant 降级后封锁失败回滚状态
  - retry-block 对已有中间态强制封锁
  - 关联文件：`compliance_service.py`、`main.py`

- **白名单终端稳定性修复**：
  - 白名单匹配时直接设置 `compliance_status='bypass'` 并清零确认计数
  - 增加周期性全量重算，修复卡死状态
  - 消除震荡闭环，保持稳定的 bypass 状态
  - 关联文件：`compliance_service.py`、`main.py`
```

#### 1.2 更新 VERSION 文件

编辑 `VERSION` 文件内容为：
```
3.13.0
```

---

### 步骤 2：分批次提交推送

#### 批次 1：文档和版本更新
```bash
git add docs/changelog.md
git add VERSION
git commit -m "chore(release): prepare v3.13.0 - update changelog and version"
git push origin develop
```

#### 批次 2：compliance_service 修复
```bash
git add backend/app/services/compliance_service.py
git commit -m "fix(compliance): strengthen state invariant - non_compliant implies blocked"
git push origin develop
```

#### 批次 3：对账服务修复
```bash
git add backend/app/services/firewall_reconciliation_service.py
git commit -m "fix(reconciliation): remove duplicate cache logic, use main.py implementation"
git push origin develop
```

#### 批次 4：终端服务修复
```bash
git add backend/app/services/terminal_service.py
git commit -m "fix(terminals): update stats logic to count blocked only"
git push origin develop
```

#### 批次 5：其他后端修复
```bash
git add backend/app/api/v1/endpoints/system.py
git commit -m "feat(system): cache reconciliation result for manual trigger"

git add backend/app/main.py
git commit -m "feat(main): add _cache_reconcile_result function"

git add backend/app/services/sangfor_service.py
git commit -m "fix(sangfor): minor fixes for firewall integration"
git push origin develop
```

#### 批次 6：前端修复
```bash
git add frontend/src/pages/Blacklist.tsx
git commit -m "feat(blacklist): add firewall actual blocked stats display"

git add frontend/src/pages/Terminals.tsx
git commit -m "fix(terminals): use stats.blocked for non-compliant count"

git add frontend/src/hooks/useTerminalData.ts
git commit -m "fix(terminals): update fallback stats logic"

git add frontend/src/lib/constants.ts
git commit -m "feat(frontend): add constants for new features"

git add frontend/src/i18n/locales/en.ts
git add frontend/src/i18n/locales/ja.ts
git add frontend/src/i18n/locales/zh.ts
git commit -m "i18n: update translations for new features"
git push origin develop
```

---

### 步骤 3：等待用户在 web 手动完成 PR 合并

**用户操作**：在 GitHub 上完成 PR 合并

---

### 步骤 4：在 main 分支打 tag 并同步回 develop

#### 4.1 切换到 main 并拉取
```bash
git checkout main
git pull origin main
```

#### 4.2 合并 develop
```bash
git merge --no-ff develop -m "chore(release): merge develop for v3.13.0"
```

#### 4.3 打 tag
```bash
git tag -a v3.13.0 -m "release v3.13.0: firewall actual stats display, fix inconsistent stats, stabilize whitelist terminals"
```

#### 4.4 推送 main 和 tag
```bash
git push origin main
git push origin v3.13.0
```

#### 4.5 在 GitHub 创建 Release
1. 访问 https://github.com/pinglife80/TerminalAccessManager/releases
2. 点击 "Draft a new release"
3. Choose a tag: `v3.13.0`
4. Release title: `v3.13.0`
5. 填写 Release notes（从 changelog.md 复制）
6. 点击 "Publish release"

#### 4.6 同步回 develop
```bash
git checkout develop
git merge main -m "chore(release): merge main v3.13.0 back to develop"
git push origin develop
```

---

## 3. 验证清单

- [ ] changelog.md 已更新
- [ ] VERSION 文件已更新
- [ ] 代码分批次提交推送完成
- [ ] 用户在 web 手动完成 PR 合并
- [ ] main 分支打 tag 并推送
- [ ] GitHub Release 已创建
- [ ] 同步回 develop 分支完成

---

**计划制定完成**，严格按用户要求执行。请审核后开始。
