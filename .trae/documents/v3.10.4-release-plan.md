# v3.10.4 发布计划：文档更新 + 版本升级 + 提交推送 + PR + Tag 同步

## Summary

当前 develop 分支有 5 个未提交的后端文件修改（loguru `<module>` 修复 + 通知 worker SMTP 认证错误处理改进 + 配置描述修正）。本计划将这些变更整理为 v3.10.4 patch 版本发布，包括：文档内容更新、版本号升级、代码提交、推送、手动 PR、打 tag 同步。

**版本类型**：Patch（Bug 修复，无新功能）→ v3.10.3 → **v3.10.4**

---

## Current State Analysis

### 未提交的代码变更（5 个文件）

| 文件 | 变更内容 |
|------|----------|
| backend/app/core/logging_config.py | 新增 `_patcher()` 函数，将 `<module>` 替换为 `[module]`，防止 loguru colorizer ValueError |
| backend/app/services/email_service.py | `EmailSender.send()` 新增 `smtplib.SMTPAuthenticationError` 专门捕获，标记 `AUTH_ERROR:` 前缀；`import smtplib` 移至模块级 |
| backend/app/services/notification_channels/email_channel.py | 检测 `AUTH_ERROR:` 前缀，设置 `error_code="AUTH_ERROR"` |
| backend/app/services/notification_workers.py | `_deliver_notification()` 检测 `AUTH_ERROR` 跳过重试，直接记录失败 |
| backend/app/services/config_service.py | `email_password` 配置描述从"stored encrypted in DB"改为"use authorization code/授权码 for QQ/163" |

### 当前版本信息

- VERSION: 3.10.3
- 最新 tag: v3.10.3
- 分支: develop（与 origin/develop 同步，除上述未提交变更）
- main 分支: 与 v3.10.3 tag 对齐

### 需要更新版本的文件（含 3.10.3 引用的文件）

| 文件 | 更新内容 |
|------|----------|
| VERSION | 3.10.3 → 3.10.4 |
| frontend/package.json | version 字段 3.10.3 → 3.10.4 |
| frontend/package-lock.json | version 字段 3.10.3 → 3.10.4 |
| docs/changelog.md | 头部版本号 + 新增 [3.10.4] 版本块 |
| docs/release-notes.md | 头部版本号 + 新增 [v3.10.4] 版本块 |
| docs/deployment.md | 头部版本号 |
| docs/branding.md | 头部版本号 |
| docs/production-readiness-assessment.md | 头部版本号 + 跟踪表新增行 |

### 需要内容更新的文档

| 文档 | 更新内容 |
|------|----------|
| docs/changelog.md | 新增 [3.10.4] 版本块，记录 3 项修复 |
| docs/release-notes.md | 新增 [v3.10.4] 版本块，含修改文件表 |
| docs/logging-guide.md | 补充 `_patcher` 机制说明（防止 `<module>` colorizer 错误） |
| docs/user-guide.md | 修正 L1092 和 L1103 关于 SMTP 密码"加密存储"的误导描述 |
| docs/operations-runbook.md | 新增"通知发送失败（SMTP 认证错误）"故障排查条目 |

---

## Proposed Changes

### Step 1: 文档内容更新

#### 1.1 docs/changelog.md — 新增 [3.10.4] 版本块

在 `## [3.10.3]` 之前插入新版本块，头部版本号更新为 v3.10.4：

```markdown
## [3.10.4] - 2026-08-10

### 修复

- **Loguru 控制台日志 ValueError**：模块级代码（如 `main.py` 的 Prometheus 初始化）触发日志时，`{function}` 字段值为 `<module>`，loguru colorizer 将其解析为颜色指令标签并抛出 `ValueError: Tag '<module>' interpreted as a color directive`
  - 修复方案：新增 `_patcher()` 函数，通过 `logger.configure(patcher=...)` 在格式化前将 `<module>` 中的尖括号替换为方括号 `[module]`
  - 关联文件：backend/app/core/logging_config.py

- **通知 worker SMTP 认证错误无脑重试**：邮件通知发送失败时，SMTP 认证错误（"invalid username-password pair or user is disabled"）被统一包装为 `EmailSendError`，通知 worker 无法区分认证失败与瞬时错误，对永久性认证错误执行无意义的指数退避重试（3 次，最多 40 秒），浪费资源并刷屏日志
  - 修复方案：`EmailSender.send()` 新增 `smtplib.SMTPAuthenticationError` 专门捕获，标记 `AUTH_ERROR:` 前缀；`EmailChannel` 传播 `error_code="AUTH_ERROR"`；`NotificationWorkers._deliver_notification()` 检测到 `AUTH_ERROR` 跳过重试，直接记录失败并输出引导用户检查 SMTP 凭证的警告
  - 关联文件：backend/app/services/email_service.py、backend/app/services/notification_channels/email_channel.py、backend/app/services/notification_workers.py

### 改进

- **邮件密码配置描述修正**：`email_password` 系统配置项描述从"stored encrypted in DB"改为"use authorization code/授权码 for QQ/163"，消除误导（密码实际以明文存储于 system_config 表）
  - 关联文件：backend/app/services/config_service.py
```

#### 1.2 docs/release-notes.md — 新增 [v3.10.4] 版本块

在 `## [v3.10.3]` 之前插入新版本块，头部版本号更新为 v3.10.4：

```markdown
## [v3.10.4] - 2026-08-10

### 后端日志与通知错误修复包

#### 背景

v3.10.3 发布后发现两个后端日志错误：
1. Loguru 在模块级代码触发日志时抛出 ValueError（`<module>` 被解析为颜色指令）
2. 通知 worker 对 SMTP 认证错误执行无意义重试，日志刷屏

#### 修改文件

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| backend/app/core/logging_config.py | 新增 | `_patcher()` 函数 + `logger.configure(patcher=...)` 注册 |
| backend/app/services/email_service.py | 修改 | 新增 `SMTPAuthenticationError` 捕获 + `AUTH_ERROR:` 标记；`import smtplib` 移至模块级 |
| backend/app/services/notification_channels/email_channel.py | 修改 | 检测 `AUTH_ERROR:` 前缀，设置 `error_code="AUTH_ERROR"` |
| backend/app/services/notification_workers.py | 修改 | `_deliver_notification()` 检测 `AUTH_ERROR` 跳过重试 |
| backend/app/services/config_service.py | 修改 | `email_password` 配置描述修正 |
| docs/changelog.md | 更新 | 新增 [3.10.4] 版本块 |
| docs/release-notes.md | 更新 | 新增 [v3.10.4] 版本块 |
| docs/logging-guide.md | 更新 | 补充 `_patcher` 机制说明 |
| docs/user-guide.md | 更新 | 修正 SMTP 密码"加密存储"误导描述 |
| docs/operations-runbook.md | 更新 | 新增通知 SMTP 认证错误排查条目 |
| docs/deployment.md | 更新 | 文档版本号 |
| docs/branding.md | 更新 | 文档版本号 |
| docs/production-readiness-assessment.md | 更新 | 文档版本号 + 跟踪表 |
| VERSION | 更新 | 3.10.3 → 3.10.4 |
| frontend/package.json | 更新 | 3.10.3 → 3.10.4 |
| frontend/package-lock.json | 更新 | 3.10.3 → 3.10.4 |

#### 验证

- 模块级日志（如 "Prometheus metrics enabled..."）正常输出 `[module]` 而非 `<module>`，无 ValueError
- SMTP 认证失败时通知 worker 跳过重试，日志输出 "Skipping retry for email: SMTP auth failed"
- 瞬时错误（网络超时等）仍正常触发重试机制
```

#### 1.3 docs/logging-guide.md — 补充 patcher 机制说明

在日志格式说明部分（3.2 节附近）补充关于 `_patcher` 的说明：

```markdown
**Colorizer 安全处理**：

Python 模块级代码的函数名为 `<module>`，loguru colorizer 会将其解析为颜色指令标签导致 `ValueError`。
`logging_config.py` 通过 `logger.configure(patcher=_patcher)` 注册补丁函数，在格式化前将 `<module>` 替换为 `[module]`，确保控制台彩色输出安全。
```

#### 1.4 docs/user-guide.md — 修正 SMTP 密码描述

- L1092: `| 密码 | SMTP 认证密码（加密存储） | 否 |` → `| 密码 | SMTP 认证密码（QQ/163 邮箱请使用授权码） | 否 |`
- L1103: `> **安全提示**：SMTP 密码在数据库中以加密形式存储（ENC: 前缀），不会明文暴露。` → `> **提示**：QQ/163 等邮箱服务商需使用授权码而非账户密码。SMTP 密码以明文存储于系统配置表，请确保数据库访问权限受控。`

#### 1.5 docs/operations-runbook.md — 新增通知 SMTP 认证错误排查

在"## 2. 常见故障排查"末尾（2.8 之后）新增 2.9：

```markdown
### 2.9 通知发送失败（SMTP 认证错误）

**故障现象：**
- 通知日志中出现 "AUTH_ERROR: SMTP authentication failed"
- 通知 worker 日志 "Skipping retry for email: SMTP auth failed. Fix email credentials in Settings -> Email Settings."
- 邮件通知全部失败，其他渠道（钉钉/企业微信）正常

**可能原因：**
1. SMTP 用户名/密码错误
2. QQ/163 等邮箱使用了账户密码而非授权码（授权码需在邮箱网页端设置中生成）
3. 邮箱账户被禁用或锁定

**排查步骤：**
1. 进入 系统设置 → 邮件设置，确认 SMTP 主机/端口/用户名/密码配置正确
2. 使用"测试连接"功能验证 SMTP 配置
3. 如使用 QQ/163 邮箱，确认填写的是授权码而非登录密码
4. 检查邮箱账户是否被锁定或禁用

**修复方式：**
- 更新正确的 SMTP 凭证并保存
- 系统会自动跳过认证错误通知的重试，无需手动清理重试队列
```

### Step 2: 版本号升级

更新以下文件中的版本号 3.10.3 → 3.10.4：

| 文件 | 操作 |
|------|------|
| VERSION | 替换内容为 `3.10.4` |
| frontend/package.json | `"version": "3.10.3"` → `"version": "3.10.4"` |
| frontend/package-lock.json | `"version": "3.10.3"` → `"version": "3.10.4"`（注意有多个 version 字段，仅改顶层和 packages[""].version） |
| docs/changelog.md | 头部 `v3.10.3` → `v3.10.4` |
| docs/release-notes.md | 头部 `v3.10.3` → `v3.10.4` |
| docs/deployment.md | 头部 `v3.10.3` → `v3.10.4` |
| docs/branding.md | 头部 `v3.10.3` → `v3.10.4` |
| docs/production-readiness-assessment.md | 头部 `v3.10.3` → `v3.10.4` + 跟踪表新增 v3.10.4 行 |

### Step 3: 代码提交

在 develop 分支上提交所有变更（代码 + 文档 + 版本号）：

```bash
git add backend/app/core/logging_config.py \
        backend/app/services/email_service.py \
        backend/app/services/notification_channels/email_channel.py \
        backend/app/services/notification_workers.py \
        backend/app/services/config_service.py \
        docs/changelog.md docs/release-notes.md docs/logging-guide.md \
        docs/user-guide.md docs/operations-runbook.md \
        docs/deployment.md docs/branding.md docs/production-readiness-assessment.md \
        VERSION frontend/package.json frontend/package-lock.json

git commit -m "fix(logging,notification): loguru <module> colorizer crash + SMTP auth error handling (v3.10.4)"
```

Commit message 说明：
- type: `fix`（bug 修复）
- scope: `logging,notification`（两个模块）
- subject: 概述两个修复 + 版本号

### Step 4: 推送 develop

```bash
git push origin develop
```

### Step 5: 手动 PR（develop → main）

在 GitHub 网页端手动创建 Pull Request：

1. 访问 `https://github.com/pinglife80/TerminalAccessManager/compare/main...develop`
2. **Title**: `fix: v3.10.4 — loguru <module> colorizer crash + SMTP auth error handling`
3. **Description**:
   ```
   ## v3.10.4 Patch Release

   ### 修复
   - Loguru 控制台日志 ValueError（<module> 被 colorizer 解析为颜色指令）
   - 通知 worker 对 SMTP 认证错误无脑重试（新增 AUTH_ERROR 检测，跳过重试）

   ### 改进
   - 邮件密码配置描述修正（消除"加密存储"误导）

   ### 文档
   - changelog.md、release-notes.md 新增 v3.10.4 版本块
   - logging-guide.md 补充 patcher 机制说明
   - user-guide.md 修正 SMTP 密码描述
   - operations-runbook.md 新增通知 SMTP 认证错误排查条目

   ### 版本
   - 3.10.3 → 3.10.4
   ```
4. 确认 CI 检查通过
5. 点击 **Merge pull request**（Create a merge commit）

### Step 6: 打 Tag

PR 合并到 main 后，在 main 上打 tag：

```bash
git checkout main
git pull origin main
git tag -a v3.10.4 -m "release v3.10.4: loguru <module> colorizer crash fix + SMTP auth error handling"
git push origin v3.10.4
```

### Step 7: 同步 develop

```bash
git checkout develop
git merge main
git push origin develop
```

---

## Assumptions & Decisions

1. **版本号 v3.10.4（Patch）**：本次仅为 bug 修复和文档更新，无新功能、无破坏性变更，符合 Patch 版本递增。
2. **单次 commit**：所有变更（代码 + 文档 + 版本号）合并为单个 commit，因为它们属于同一个发布周期。遵循项目历史风格（如 `e43dd15 fix(infra): ... (v3.10.3)`）。
3. **手动 PR**：用户明确要求手动 PR，因此在 push develop 后提供 GitHub 网页操作指引，不使用 API 自动创建。
4. **develop 直接提交**：根据 git-workflow-guide.md 5.2 节"单人开发简化模式"，小改动（<5 commits）可直接在 develop 上提交。本次仅 1 个 commit，无需走 feature 分支。
5. **user-guide.md 加密描述修正**：文档原称"SMTP 密码以加密形式存储（ENC: 前缀）"，但实际代码中 `email_password` 以明文存储于 system_config 表。修正为准确描述，避免误导用户。
6. **operations-runbook.md 新增 2.9 节**：通知 SMTP 认证错误是常见运维问题，新增排查条目提升可运维性。

---

## Verification Steps

1. **提交前检查**：`git diff --stat` 确认 16 个文件变更（5 代码 + 8 文档 + 3 版本号）
2. **版本号一致性**：`grep -rn "3.10.4" VERSION frontend/package.json docs/changelog.md docs/release-notes.md docs/deployment.md docs/branding.md docs/production-readiness-assessment.md` 确认所有文件版本已更新
3. **编译检查**：`cd backend && .venv/bin/python -c "import py_compile; [py_compile.compile(f, doraise=True) for f in ['app/core/logging_config.py', 'app/services/email_service.py', 'app/services/notification_channels/email_channel.py', 'app/services/notification_workers.py', 'app/services/config_service.py']]"`（已验证通过）
4. **推送后检查**：GitHub PR 页面确认 CI 全部通过
5. **Tag 后检查**：`git tag -l v3.10.4` 确认 tag 存在，`git log --oneline v3.10.3..v3.10.4` 确认包含本次提交
6. **同步后检查**：`git log --oneline -1 develop` 和 `git log --oneline -1 main` 指向同一 commit
