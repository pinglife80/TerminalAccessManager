# 发布同步计划 v3.20.0（Minor）

> 文档版本：v3.20.0  更新日期：2026-09-04

## 一、概述

本计划将自 v3.19.0 发布（tag `v3.19.0` 已存在于 `main`，merge commit `ed77c17` / PR #46）以来积累在 `develop` 工作区的**未提交改动**，按 [git-workflow-guide.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/git-workflow-guide.md) 单人开发最佳实践完成：**文档更新 → 版本号递增 → 代码提交 → 推送 develop → 手动 PR → 打 tag → develop 同步**。

主线为：**MAC+IP 复合唯一键改造（迁移 040）+ 合规状态转换优化 + 白名单匹配统一 + 白名单变更通知补全 + 黑名单统计口径 UI 说明**。

- **版本号**：`v3.20.0`（Minor — 含数据库迁移 040 与多项新能力/新可配置项，见 2.3）
- **更新日期**：`2026-09-04`
- **发布分支**：`develop` → `main`
- **tag**：`v3.20.0`（annotated tag，仅打于 `main`，PR 合并后）

***

## 二、当前状态分析（发布依据）

### 2.1 仓库状态

| 项目 | 状态 |
| --- | --- |
| 当前分支 | `develop`，HEAD = `ed77c17`（Merge PR #46，即 v3.19.0 发布点），与 `origin/develop` 同点 |
| `main` | `ed77c17`（Merge PR #46），与 `origin/main` 同点 |
| 最新 tag | `v3.19.0`（annotated，指向 main） |
| `VERSION` 文件 | `3.19.0`（待更新为 `3.20.0`） |
| 未提交改动 | **20 个已修改文件 + 5 个未跟踪文件**（迁移 040 + 4 个规划文档），另 1 个新测试文件被 `.gitignore` 忽略需强制加入 |

> ✅ 改动**已在 `develop` 工作区**，无需分支迁移，直接按「五、提交计划」在 `develop` 上提交。

### 2.2 待发布改动清单（五条主线）

#### 2.2.1 MAC+IP 复合唯一键改造（迁移 040，架构级）

| 文件 | 变更要点 |
| --- | --- |
| `backend/alembic/versions/040_terminal_mac_ip_unique.py`（新增） | `terminals` 唯一约束由 `uq_terminal_mac` 改为 `uq_terminal_mac_ip`(ip_address, mac_address_normalized)；`mac_address_normalized` 置为 NOT NULL 并删除历史 NULL-MAC 行 |
| `backend/app/models/terminal.py` | `mac_address_normalized` 改 NOT NULL；`__table_args__` 唯一约束替换为 `uq_terminal_mac_ip` |
| `backend/app/services/arp_collector_service.py` | 批内去重由 `seen_macs` 改 `seen_pairs`(MAC,IP)；upsert/合规结果 lookup 改用 (MAC,IP) 复合键，消除同 MAC 多 IP 折叠漏采 |
| `backend/app/services/data_source_service.py` | `safe_delete_binding` 由 MAC 定位改为 (MAC,IP) 复合定位，`scalar_one_or_none()` 改 `scalars().first()` 避免 MultipleResultsFound |
| `backend/app/services/firewall_reconciliation_service.py` | 对账终端关联、补建回写均补 `Blacklist.ip_address == Terminal.ip_address` 复合定位 |
| `backend/app/services/terminal_service.py` | `get_stats` 注释与黑名单 join（`_blacklist_terminal_join`/`_blacklist_terminal_key`）改为 (MAC,IP) 复合口径 |
| `frontend/src/pages/Terminals.tsx` | 黑名单命中改为 **IP 优先**（生成 `both`/`ip`，移除纯 `mac` 命中），解封标识符改用 `ip_address`；避免同 MAC 不同 IP 误判 |

#### 2.2.2 合规状态转换优化

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/services/compliance_service.py` | 命中 IPGuard 立即升级 `compliant`（去升级方向确认次数）；新增 `_get_compliant_downgrade_threshold` 独立降级阈值；`apply_manual_whitelist_for_terminal`（手动白名单立即 bypass，见 2.2.3） |
| `backend/app/services/config_service.py` | 新增可配置项 `compliance_compliant_downgrade_threshold`（默认 6，2-20） |
| `backend/app/schemas/system_config.py` | `ComplianceConfigResponse` 暴露该字段 |
| `backend/tests/test_compliance_service.py` | 补充即时升级 + 独立降级阈值 3 个用例 |
| `frontend/src/hooks/useTerminalData.ts` | `ComplianceConfig` 接口新增该字段 |
| `frontend/src/pages/GeneralSettings.tsx` | compliance 分组新增该配置项 |
| `frontend/src/i18n/locales/{en,zh,ja}.ts` | 新增该配置项文案（同时含 2.2.5 黑名单 hint 文案，跨主题） |

#### 2.2.3 白名单匹配逻辑统一

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/services/terminal_service.py` | `add_to_whitelist` 匹配逻辑统一为 `scalars().all()` 循环 apply；`wl_match_type` 取值修正为 `"mac"/"ip"/"both"`；manual whitelist 立即生效路径 |
| `backend/app/services/compliance_service.py` | `apply_manual_whitelist_for_terminal` 按 `wl_match_type` 写入 `terminal.wl_match_type` |
| `backend/tests/test_terminal_service.py`（新增，需 `git add -f`） | 新增 `TestAddToWhitelistMatching` 3 个用例，覆盖 mac_only/ip_only/both 同 MAC 多 IP 场景 |

#### 2.2.4 白名单变更通知补全（问题 2）

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/services/notification_channels/event_types.py` | 新增 `EventType.WHITELIST_CHANGED = "admin.whitelist_changed"` 及元数据；新增 `DEFAULT_SUBSCRIBED_EVENTS` 默认订阅种子 |
| `backend/app/services/event_emitter.py` | 新增 `emit_whitelist_changed` 发射器 |
| `backend/app/services/notification_service.py` | 新增 `ensure_default_subscriptions`（幂等回填缺失订阅，不删除既有）；创建渠道无 `events` 时默认订阅全部事件 |
| `backend/app/main.py` | lifespan 在 `initialize_channels` 前调用 `ensure_default_subscriptions` |

#### 2.2.5 黑名单统计口径 UI 说明（问题 3 方案 A）

| 文件 | 变更要点 |
| --- | --- |
| `frontend/src/pages/Blacklist.tsx` | 成功封锁 / 待解封卡片新增 `Info` 图标 tooltip，说明 `pending_retry_unblock ⊆ success_blocked` 子集关系 |
| `frontend/src/i18n/locales/{en,zh,ja}.ts` | 新增 hint 文案（已归入 2.2.2 的 i18n 文件） |

#### 2.2.6 新增规划文档（未跟踪，纳入版本控制）

| 路径 | 说明 |
| --- | --- |
| `.trae/documents/系统诊断_ARP漏采_通知缺失_双清单根因.md` | 三问题根因诊断 |
| `.trae/documents/三问题优化解决方案与实施报告.md` | 三问题方案报告 |
| `.trae/documents/MAC+IP复合唯一键改造-回归测试用例.md` | 回归测试用例 |
| `.trae/documents/终端合规状态转换时序分析.md` | 合规时序分析 |
| `.trae/documents/发布同步计划-v3.20.0.md` | 本计划文档 |

### 2.3 改动性质（决定 Minor 版本）

含 1 项数据库迁移（040，唯一约束变更）+ 架构级改动（终端唯一标识 MAC → MAC+IP）+ 多项新能力/新可配置项（合规即时转换、`compliance_compliant_downgrade_threshold`、`admin.whitelist_changed` 通知、黑名单口径 UI 说明），故按语义化版本 **Minor 递增至 `v3.20.0`**；符合「迁移 038→v3.18.0，迁移 039→v3.19.0」的既有节奏。

***

## 三、版本更新

| 文件 | 当前 | 目标 |
| --- | --- | --- |
| `VERSION` | `3.19.0` | `3.20.0` |

> `VERSION` 是唯一版本源；`package.json` / `package-lock.json` 由前端镜像构建期动态同步，**无需手动改**（沿用既有约定）。

***

## 四、项目文档内容更新

遵循既有约定：文档头格式 `> 文档版本：{version}  更新日期：{date}`；`production-readiness-assessment.md` 仅追加跟踪表行、header 不 bump。

| 文档 | 更新方式 |
| --- | --- |
| `docs/changelog.md` | header `v3.19.0`→`v3.20.0`；顶部新增 `## [3.20.0] - 2026-09-04` 条目，按「新增/修复」分类 |
| `docs/release-notes.md` | header `v3.19.0`→`v3.20.0`；顶部新增 `## [v3.20.0] - 2026-09-04` 章节（背景/主要变更表/升级步骤/升级注意事项） |
| `docs/production-readiness-assessment.md` | **仅**在文档版本跟踪表顶部追加 1 行 `v3.20.0`；header 保持 `v3.10.4` 不 bump |
| `docs/business-workflow.md` | header `v3.19.0`→`v3.20.0`；另做**轻量正文更新**：§2 终端数据模型唯一约束 `uq_terminal_mac`→`uq_terminal_mac_ip`（约 L82-96）与「v3.20 重要变更」注记；§8 关键参数补充 `compliance_compliant_downgrade_threshold`；合规转换语义（IPGuard 命中即时 compliant、白名单即时 bypass） |

### 4.1 待写入 changelog / release-notes 的变更条目

**新增（Features）**：

1. **MAC+IP 复合唯一键（迁移 040）**：`terminals` 唯一标识由 `mac_address_normalized` 改为 `(ip_address, mac_address_normalized)`，同 MAC 多 IP（如桥接虚拟机）独立成行，消除 ARP 折叠导致的漏采；`mac_address_normalized` 置 NOT NULL。关联：`040_terminal_mac_ip_unique.py`、`models/terminal.py`、`arp_collector_service.py`、`data_source_service.py`、`firewall_reconciliation_service.py`、`terminal_service.py`、`Terminals.tsx`
2. **合规状态转换优化**：命中 IPGuard 立即升级 `compliant`、白名单手动新增立即 `bypass`（均去确认次数防抖）；新增可配置项 `compliance_compliant_downgrade_threshold`（默认 6，2-20）独立控制 `compliant → non_compliant` 降级，合规终端更稳定。关联：`compliance_service.py`、`config_service.py`、`schemas/system_config.py`、`useTerminalData.ts`、`GeneralSettings.tsx`、`i18n/locales/{en,zh,ja}.ts`
3. **白名单变更通知**：新增事件类型 `admin.whitelist_changed` 与发射器 `emit_whitelist_changed`；通知渠道创建/启动时幂等回填默认订阅，避免「已发射但无渠道订阅」静默丢信。关联：`event_emitter.py`、`notification_channels/event_types.py`、`notification_service.py`、`main.py`
4. **黑名单统计口径 UI 说明**：成功封锁 / 待解封卡片增加 tooltip，说明 `pending_retry_unblock ⊆ success_blocked` 子集关系。关联：`Blacklist.tsx`、`i18n/locales/{en,zh,ja}.ts`

**修复（Fixes）**：

1. **白名单匹配逻辑统一**：`add_to_whitelist` 统一 `scalars().all()` 循环 apply，`wl_match_type` 修正为 `"mac"/"ip"/"both"`，修复同 MAC 多 IP 场景匹配不完整。关联：`terminal_service.py`、`compliance_service.py`
2. **前端黑名单匹配 IP 优先**：移除纯 `mac` 命中，仅生成 `both`/`ip`，解封标识符改用 `ip_address`，避免同 MAC 不同 IP 终端被误判为已封堵。关联：`Terminals.tsx`

***

## 五、代码提交计划（7 个提交，在 develop 上完成）

按主题分组，遵循 Conventional Commits（`type(scope): subject`）。提交顺序 1→7 依次执行，每个文件仅出现在一个提交中。

> 跨主题文件说明：`terminal_service.py`（含 MAC+IP join 与白名单统一，归提交 2）、`compliance_service.py`（含合规优化与 `apply_manual_whitelist_for_terminal`，归提交 3）、`i18n/locales/*`（含合规阈值与黑名单 hint 文案，归提交 3，提交 5 的 `Blacklist.tsx` 仅引用 hint key）——均按「主主题」归入单一提交，避免分步 hunk。

| # | 提交内容（文件） | Commit Message |
| --- | --- | --- |
| 1 | `backend/alembic/versions/040_terminal_mac_ip_unique.py`、`backend/app/models/terminal.py` | `chore(database): migration 040 switch terminal unique key to (IP, MAC)` |
| 2 | `backend/app/services/arp_collector_service.py`、`backend/app/services/data_source_service.py`、`backend/app/services/firewall_reconciliation_service.py`、`backend/app/services/terminal_service.py`、`frontend/src/pages/Terminals.tsx`、`backend/tests/test_terminal_service.py`（`git add -f`） | `feat(terminals): one-MAC multi-IP composite identity, IP-first matching and unified whitelist apply` |
| 3 | `backend/app/services/compliance_service.py`、`backend/app/services/config_service.py`、`backend/app/schemas/system_config.py`、`backend/tests/test_compliance_service.py`、`frontend/src/hooks/useTerminalData.ts`、`frontend/src/pages/GeneralSettings.tsx`、`frontend/src/i18n/locales/{en,zh,ja}.ts` | `feat(compliance): immediate bypass/compliant transitions and independent downgrade threshold` |
| 4 | `backend/app/main.py`、`backend/app/services/event_emitter.py`、`backend/app/services/notification_channels/event_types.py`、`backend/app/services/notification_service.py` | `feat(notifications): add whitelist_changed event and default subscription backfill` |
| 5 | `frontend/src/pages/Blacklist.tsx` | `fix(blacklist): clarify dual-list stat overlap with tooltips` |
| 6 | `.trae/documents/` 5 个规划文档（4 个既有报告/用例 + 本计划） | `docs: add v3.20.0 MAC+IP key, compliance optimization and notification planning documents` |
| 7 | `VERSION`、`docs/changelog.md`、`docs/release-notes.md`、`docs/production-readiness-assessment.md`、`docs/business-workflow.md` | `chore(release): bump version to v3.20.0 and refresh docs` |

***

## 六、推送与 PR

1. **提交前验证**（在 develop 上，本地）：
   ```bash
   ./manage.sh update          # 本地重建 + 重启，确认构建/TS 编译无错
   docker exec tam_backend python -m pytest -q   # 后端回归（预期全绿，含新增 matching/compliance 用例）
   ```

2. **按「五、提交计划」依次 commit**，每个提交后用 `git status` 确认无漏提/误提：
   - 迁移 040 与规划文档为**未跟踪**文件，`git add` 正常加入。
   - `backend/tests/test_terminal_service.py` 被 `.gitignore:84` 的 `test_*.py` 规则忽略，须 **`git add -f`** 强制加入（与既有 `test_*.py` 文件历史做法一致）。

3. **推送 develop**：`git push origin develop`（含提交 1–7，共 7 个）。

4. **手动创建 PR**（GitHub：`develop` → `main`）：
   - 标题：`release: v3.20.0 MAC+IP 复合唯一键、合规转换优化与白名单通知补全`
   - 描述：粘贴 `docs/changelog.md` 的 `[3.20.0]` 条目
   - 复核 `backend-lint / backend-test / frontend-lint / frontend-test` 四项 CI 均通过
   - 自审 diff 后选择 **Merge**（Merge commit，沿用既有习惯；不 squash、不删除 develop）

***

## 七、打 tag 与 develop 同步

> 依据项目约束「Git tag 仅打于 main、且须 PR 合并后」。

1. PR 合并后，本地同步并切到 main：
   ```bash
   git checkout main && git pull origin main
   ```
2. 打 annotated tag 并推送：
   ```bash
   git tag -a v3.20.0 -m "release v3.20.0: MAC+IP composite identity, compliance transition optimization and whitelist notifications"
   git push origin v3.20.0
   ```
3. develop 同步 main（含 tag）：
   ```bash
   git checkout develop && git merge main && git push origin develop
   ```

***

## 八、假设与决策

1. **版本号**：`v3.20.0`（Minor）——含迁移 040 唯一约束变更、终端唯一标识架构升级及多项新能力/新可配置项，其余为向后兼容修复。
2. **文档命名**：`发布同步计划-v3.20.0.md`——与版本号一致，延续既有《发布同步计划-vX.Y.Z.md》命名。
3. **变更范围**：五条主线（MAC+IP 复合键 / 合规转换优化 / 白名单匹配统一 / 白名单通知 / 黑名单口径 UI）合并为一次发布。
4. **提交粒度**：7 个提交（迁移+模型 / 终端复合键+白名单统一 / 合规后端+前端 / 通知后端 / 黑名单 UI / 规划文档 / 版本发布）。
5. **`production-readiness-assessment.md` header 保持 v3.10.4 不 bump**，仅追加跟踪表行（沿用既有约定）。
6. **`package.json` 不改**：由前端镜像构建期从 `VERSION` 动态同步。
7. **PR 采用手动创建**（用户明确「手动 PR」），合并动作在 GitHub 上完成。
8. **规划文档纳入版本控制**：4 个诊断/报告/用例文档 + 本计划一并提交。
9. **`backend/tests/test_terminal_service.py` 需 `git add -f`**：受 `.gitignore:test_*.py` 规则影响，属历史遗留宽规则，沿用既有测试文件强制加入做法。
10. **`business-workflow.md` 做轻量正文更新**：本次终端唯一标识变更与合规转换语义改变，需在 §2 数据模型、§8 关键参数同步反映（不涉及顶层流程变更）。

***

## 九、验证步骤（发布后）

1. `git tag -l | tail` 确认 `v3.20.0` 存在。
2. `git log v3.19.0..v3.20.0 --oneline` 核对版本间提交与文件清单（7 个提交）。
3. `git describe --tags --abbrev=0` 返回 `v3.20.0`。
4. `./manage.sh health` 确认服务健康、版本号已刷新为 `3.20.0`。
5. 迁移验证：`./manage.sh upgrade` 执行 `alembic upgrade head` 后，`docker exec tam_backend alembic current` 应显示 `040_terminal_mac_ip_unique`。
6. 业务链抽查：
   - 同 MAC 多 IP（如桥接虚拟机）终端在终端页 / 看板 / 黑名单页状态隔离，无跨 IP 状态串扰。
   - 白名单三种匹配分支（mac_only / ip_only / both）手动新增均立即 `bypass`，`wl_match_type` 展示正确。
   - 系统设置「合规降级阈值」可配置，合规终端降级走独立阈值。
   - 白名单新增/删除后，已订阅渠道能收到 `白名单变更` 通知；无显式订阅的新建渠道默认订阅全部事件。