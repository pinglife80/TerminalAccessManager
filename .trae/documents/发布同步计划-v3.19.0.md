# 发布同步计划 v3.19.0（Minor）

> 文档版本：v3.19.0  更新日期：2026-09-04

## 一、概述

本计划将自 v3.18.0 发布（tag `v3.18.0` 已存在于 `main`，merge commit `13d76b1` / PR #45）以来积累在 `develop` 工作区的未提交改动，按 [git-workflow-guide.md](file:///home/dada/Codespace/TraeCN/TerminalAccessManager/docs/git-workflow-guide.md) 单人开发最佳实践完成：**文档更新 → 版本号递增 → 代码提交 → 推送 develop → 手动 PR → 打 tag → develop 同步**。

主线为：**合规作用域 OR 匹配策略（新功能）+ 通知事件系统审计修复 + 非合规原因标记 `non_compliant_type`（迁移 039）**。

- **版本号**：`v3.19.0`（Minor — 因含「合规作用域 OR 匹配策略」新能力，IP 或 MAC 任一命中即合规）
- **更新日期**：`2026-09-04`
- **发布分支**：`develop` → `main`
- **tag**：`v3.19.0`（annotated tag，仅打于 `main`，PR 合并后）

***

## 二、当前状态分析（发布依据）

### 2.1 仓库状态

| 项目 | 状态 |
| --- | --- |
| 当前分支 | `develop`，HEAD = `13d76b1`（v3.18.0 合并提交），与 `origin/develop` 同点 |
| `main` | `13d76b1`（Merge PR #45），与 `origin/main` 同点 |
| 最新 tag | `v3.18.0`（annotated，指向 main） |
| `VERSION` 文件 | `3.18.0`（待更新为 `3.19.0`） |
| 未提交改动 | **30 个文件已修改/删除 + 6 个未跟踪**（5 个规划文档 + 1 个迁移 039），全部位于 `develop` 工作区 |

> ✅ 本次改动**已在 `develop` 工作区**，无需分支迁移，直接按「五、提交计划」在 `develop` 上提交即可。

### 2.2 待发布改动清单（三条主线）

#### 2.2.1 合规作用域 OR 匹配策略（新功能，Minor 依据）

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/models/compliance_scope.py` | docstring 补充 `ip_cidr_any`/`ip_range_any`/`mac_prefix_any` 三类 OR 语义 |
| `backend/app/schemas/compliance_scope.py` | `ScopeType` Literal 扩展 3 类 `_any` 值 |
| `backend/app/services/compliance_scope_service.py` | `_validate_scope_value` 复用 CIDR/范围/MAC 前缀校验分支，接纳 3 类新值 |
| `backend/app/services/compliance_service.py` | 新增 `_match_ipguard_or_in_memory`、`_check_terminal_in_or_scope`；`_build_unblock_reason` 签名新增 `use_or_match` 并扩展逻辑；7 处判定点接入「OR > IP-only > AND」三分支 |
| `frontend/src/api/complianceScope.ts` | `ScopeType` 联合类型扩展 3 类 `_any` |
| `frontend/src/pages/ComplianceScope.tsx` | label/desc/placeholder 与增改 `<select>` 各追加 3 类选项 |
| `frontend/src/i18n/locales/{en,zh,ja}.ts` | 新增 6 个 `complianceScope.*Any*` 文案键 |

#### 2.2.2 通知事件系统审计修复

| 文件 | 变更要点 |
| --- | --- |
| `backend/app/api/v1/endpoints/auth.py` | 修复角色指派处 `emit_role_changed` 调用签名 |
| `backend/app/api/v1/endpoints/roles.py` | 修复角色创建/更新/删除与权限变更处发射器调用签名 |
| `backend/app/api/v1/endpoints/settings.py` | 修复配置变更批量/单条 `emit_config_changed` 调用签名 |
| `backend/app/main.py` | 补 `system.datasource_sync_success` 触发点 |
| `backend/app/services/event_emitter.py` | 修正 `emit_role_changed` 等签名语义 |
| `backend/app/services/notification_aggregator.py`（删除） | legacy 聚合器旁路，整体退役 |
| `backend/app/services/notification_channels/{event_types,dingtalk,feishu,webhook,wecom}_channel.py` | `security.user_*` 分类归正；`alert.*` 注释修正；4 个渠道 `send()` 统一返回 `NotificationResult`；事件元数据去无触发点项 |
| `backend/app/services/notification_service.py` | 手动重试按 `channel_name` 隔离 |
| `backend/app/services/notification_workers.py` | 重试读取 `_channel_name`，仅重发失败渠道 |
| `backend/cli.py` | 权限/读音节同步（RBAC 幂等补种相关） |
| `backend/tests/test_notification_service.py`、`backend/tests/test_compliance_service.py` | 同步更新断言（去聚合器 patch 目标、`NotificationResult` 返回类型、mock 数据） |

#### 2.2.3 非合规原因标记 `non_compliant_type`（迁移 039）

| 文件 | 变更要点 |
| --- | --- |
| `backend/alembic/versions/039_terminal_non_compliant_type.py`（新增） | `terminals` 新增 `non_compliant_type` 列 |
| `backend/app/models/terminal.py` | 新增 `non_compliant_type` 列（`ip/mac/both/NULL`） |
| `backend/app/schemas/terminal.py` | `TerminalResponse` 暴露 `non_compliant_type` |
| `backend/app/services/compliance_service.py` | `_apply_compliance_result` 由 `ip_found/mac_found/use_ip_only` 映射写入；compliant/bypass 清空；封锁失败回滚同步清空 |
| `frontend/src/hooks/useTerminalData.ts` | `Terminal` 接口新增 `non_compliant_type` |
| `frontend/src/pages/Terminals.tsx` | 非合规徽标/详情由 `black_match_type` 改为 `non_compliant_type`；`black_match_type` 保留用于黑名单解除标识符选择 |
| `frontend/src/i18n/locales/{en,zh,ja}.ts` | 新增 `terminal.nonCompliantType` 文案 |

#### 2.2.4 新增规划文档（未跟踪，纳入版本控制）

| 路径 | 说明 |
| --- | --- |
| `.trae/documents/合规状态震荡诊断与修复方案_10.8.19.240_10.8.17.163.md` | 合规震荡诊断报告 |
| `.trae/documents/合规作用域新增OR匹配策略-实施计划.md` | OR 匹配策略实施计划 |
| `.trae/documents/通知事件系统审计与修复计划.md` | 通知系统审计计划 |
| `.trae/documents/修复方案_非合规原因标记_non_compliant_type.md` | non_compliant_type 修复计划 |
| `.trae/documents/发布同步计划-v3.19.0.md` | 本计划文档 |

### 2.3 改动性质（决定 Minor 版本）

含 1 项面向用户的新能力（合规作用域 OR 匹配策略：IP 或 MAC 任一命中即合规）及 1 项结构化新字段（`non_compliant_type`），故按语义化版本 Minor 递增至 `v3.19.0`；其余均为向后兼容的缺陷修复。

***

## 三、版本更新

| 文件 | 当前 | 目标 |
| --- | --- | --- |
| `VERSION` | `3.18.0` | `3.19.0` |

> `VERSION` 是唯一版本源；`package.json` / `package-lock.json` 由前端镜像构建期动态同步，**无需手动改**（沿用既有约定）。

***

## 四、项目文档内容更新

遵循既有约定：文档头格式 `> 文档版本：{version}  更新日期：{date}`；`production-readiness-assessment.md` 仅追加跟踪表行、header 不 bump。

| 文档 | 更新方式 |
| --- | --- |
| `docs/changelog.md` | header `v3.18.0`→`v3.19.0`、日期 `2026-09-02`→`2026-09-04`；顶部新增 `## [3.19.0] - 2026-09-04` 条目，按「新增/修复」分类 |
| `docs/release-notes.md` | header `v3.18.0`→`v3.19.0`、日期同上；顶部新增 `## [v3.19.0] - 2026-09-04` 章节，含「背景/主要变更表/升级步骤/升级注意事项」 |
| `docs/production-readiness-assessment.md` | **仅**在文档版本跟踪表顶部追加 1 行 `v3.19.0`；header 保持 `v3.10.4` 不 bump（沿用既有约定） |
| `docs/business-workflow.md` | header `v3.18.0`→`v3.19.0`、日期同上；另做**轻量正文更新**：§3.3 判定规则表新增 OR 匹配行、§3.4 Scope 类型表新增 `ip_cidr_any`/`ip_range_any`/`mac_prefix_any` 三行及说明（见 4.2） |

### 4.1 待写入 changelog / release-notes 的变更条目

**新增（Features）**：

1. **合规作用域 OR 匹配策略**：新增 `ip_cidr_any` / `ip_range_any` / `mac_prefix_any` 三种作用域类型，指定网段/前缀内的终端「IP 或 MAC 任一命中合规基线即判定 compliant」。关联：`compliance_service.py`、`models/compliance_scope.py`、`services/compliance_scope_service.py`、`schemas/compliance_scope.py`、`ComplianceScope.tsx`、`complianceScope.ts`、`i18n/locales/{en,zh,ja}.ts`
2. **非合规原因标记 `non_compliant_type`（迁移 039）**：`terminals` 新增结构化字段记录真实不合规因素（IP/MAC/BOTH），前端非合规徽标由误导性的 `black_match_type` 改为该字段展示。关联：`039_terminal_non_compliant_type.py`、`models/terminal.py`、`schemas/terminal.py`、`compliance_service.py`、`useTerminalData.ts`、`Terminals.tsx`、`i18n/locales/{en,zh,ja}.ts`

**修复（Fixes）**：

1. **通知事件系统审计修复**：退役绕过通知管道、永不真正送达的 legacy 聚合器（删除 `notification_aggregator.py`）；重试按 `_channel_name` 隔离；统一 5 类渠道 `send()` 返回 `NotificationResult`；修复 `emit_role_changed`/`emit_config_changed`/`emit_permission_changed` 调用签名错乱；补 `system.datasource_sync_success` 触发点；清理无触发点事件并归正 `security.user_*` 分类。关联：`event_emitter.py`、`endpoints/{auth,roles,settings}.py`、`main.py`、`notification_service.py`、`notification_workers.py`、`notification_channels/*`、`cli.py`

### 4.2 business-workflow.md 正文轻量更新

- §3.3 判定规则表新增一行：`在 OR 作用域内 + IP 或 MAC 任一命中 IPGuard` → `compliant`（任一命中即合规）。
- §3.4 Scope 类型表「v3.19 新增 OR 类型」追加三行：

| Scope类型 | 匹配对象 | 命中后行为 |
|-----------|---------|-----------|
| `ip_cidr_any` | 终端IP（CIDR） | IP 或 MAC 任一命中基线即合规（OR） |
| `ip_range_any` | 终端IP（范围） | IP 或 MAC 任一命中基线即合规（OR） |
| `mac_prefix_any` | 终端MAC前缀 | IP 或 MAC 任一命中基线即合规（OR） |

***

## 五、代码提交计划（6 个提交，在 develop 上完成）

按主题分组，遵循 Conventional Commits（`type(scope): subject`）。提交顺序 1→6 依次执行，每个文件仅出现在一个提交中（跨主题文件 `compliance_service.py` 已归入提交 2，避免分步 hunk）。

| # | 提交内容（文件） | Commit Message |
| --- | --- | --- |
| 1 | `backend/alembic/versions/039_terminal_non_compliant_type.py`、`backend/app/models/terminal.py` | `chore(database): migration 039 add terminal non_compliant_type column` |
| 2 | `backend/app/services/compliance_service.py`、`backend/app/models/compliance_scope.py`、`backend/app/services/compliance_scope_service.py`、`backend/app/schemas/compliance_scope.py`、`backend/app/schemas/terminal.py`、`backend/tests/test_compliance_service.py` | `feat(compliance): add OR-match scope types and non_compliant_type factor` |
| 3 | `backend/app/api/v1/endpoints/{auth,roles,settings}.py`、`backend/app/main.py`、`backend/app/services/event_emitter.py`、`backend/app/services/notification_aggregator.py`（删除）、`backend/app/services/notification_service.py`、`backend/app/services/notification_workers.py`、`backend/app/services/notification_channels/{event_types,dingtalk,feishu,webhook,wecom}_channel.py`、`backend/cli.py`、`backend/tests/test_notification_service.py` | `fix(notifications): audit event system, isolate per-channel retry and retire aggregator` |
| 4 | `frontend/src/api/complianceScope.ts`、`frontend/src/pages/ComplianceScope.tsx`、`frontend/src/hooks/useTerminalData.ts`、`frontend/src/pages/Terminals.tsx`、`frontend/src/i18n/locales/{en,zh,ja}.ts` | `feat(frontend): add OR-match scope options and non_compliant_type badge` |
| 5 | `.trae/documents/` 5 个规划文档（4 个既有计划 + 本计划） | `docs: add v3.19.0 OR-match, notification audit and non_compliant_type plan documents` |
| 6 | `VERSION`、`docs/changelog.md`、`docs/release-notes.md`、`docs/production-readiness-assessment.md`、`docs/business-workflow.md` | `chore(release): bump version to v3.19.0 and refresh docs` |

***

## 六、推送与 PR

1. **提交前验证**（在 develop 上，本地）：
   ```bash
   ./manage.sh update          # 本地重建 + 重启，确认构建/TS 编译无错
   docker exec tam_backend python -m pytest -q   # 后端回归（预期全绿）
   ```

2. **按「五、提交计划」依次 commit**，每个提交后用 `git status` 确认无漏提/误提；删除文件（`notification_aggregator.py`）用 `git rm`。

3. **推送 develop**：`git push origin develop`（含提交 1–6，共 6 个）。

4. **手动创建 PR**（GitHub：`develop` → `main`）：
   - 标题：`release: v3.19.0 合规 OR 匹配策略、通知事件系统修复与非合规原因标记`
   - 描述：粘贴 `docs/changelog.md` 的 `[3.19.0]` 条目
   - 复核 `backend-lint / backend-test / frontend-lint / frontend-test` 四项 CI 均通过
   - 自审 diff 后选择 **Merge**（Merge commit，沿用既有习惯；不 squash、不删除 develop）

***

## 七、打 tag 与 develop 同步

> 依据项目约束「Git tag 仅打于 main、且须 PR 合并后」。

1. PR 合并后，本地同步并切到 main：
   ```bash
   git checkout main && git pull origin main
   ```
2. 打 annotated tag 并推送：
   ```bash
   git tag -a v3.19.0 -m "release v3.19.0: compliance OR-match scopes, notification event audit and non_compliant_type"
   git push origin v3.19.0
   ```
3. develop 同步 main（含 tag）：
   ```bash
   git checkout develop && git merge main && git push origin develop
   ```

***

## 八、假设与决策

1. **版本号**：`v3.19.0`（Minor）——因含「合规作用域 OR 匹配策略」新能力与 `non_compliant_type` 结构化新字段，其余为向后兼容修复。
2. **文档命名**：`发布同步计划-v3.19.0.md`——与版本号一致，延续既有《发布同步计划-vX.Y.Z.md》命名。
3. **变更范围**：三条主线（OR 匹配 / 通知审计 / non_compliant_type）合并为一次发布。
4. **提交粒度**：6 个提交（迁移+模型 / 合规后端 / 通知后端 / 前端 / 规划文档 / 版本发布）。
5. **`production-readiness-assessment.md` header 保持 v3.10.4 不 bump**，仅追加跟踪表行（沿用既有约定）。
6. **`package.json` 不改**：由前端镜像构建期从 `VERSION` 动态同步。
7. **PR 采用手动创建**（用户明确「手动 PR」），合并动作在 GitHub 上完成。
8. **规划文档纳入版本控制**：5 个既有计划/诊断文档 + 本计划文档一并提交。
9. **`business-workflow.md` 做轻量正文更新**：本次新增 OR 匹配策略改变合规判定语义，需在 §3.3/§3.4 同步反映（不涉及顶层流程变更）。
10. **跨主题文件 `compliance_service.py`**：同时承载 OR 匹配与 `non_compliant_type` 改动，归入提交 2（compliance 后端），避免分步 hunk 暂存。

***

## 九、验证步骤（发布后）

1. `git tag -l | tail` 确认 `v3.19.0` 存在。
2. `git log v3.18.0..v3.19.0 --oneline` 核对版本间提交与文件清单。
3. `git describe --tags --abbrev=0` 返回 `v3.19.0`。
4. `./manage.sh health` 确认服务健康、版本号已刷新为 `3.19.0`。
5. 迁移验证：`./manage.sh upgrade` 执行 `alembic upgrade head` 后，`docker exec tam_backend alembic current` 应显示 `039_terminal_non_compliant_type`。
6. 业务链抽查：
   - 合规作用域页可新增/编辑 `ip_cidr_any` / `ip_range_any` / `mac_prefix_any` 三类，标签/说明/占位符三语正确。
   - OR 场景：构造「IP 不在基线、MAC 在基线」终端命中 OR 作用域后判 compliant；「IP/MAC 均不在基线」仍判 non_compliant 且 reason 正确。
   - 非合规徽标按真实因素显示 `(IP)/(MAC)/(BOTH)`，不再恒 `(BOTH)`。
   - 通知：配置 email+webhook 双渠道、人为让 webhook 失败，确认 email 仅发一次、webhook 进入 failed 且 `channel_name` 正确。